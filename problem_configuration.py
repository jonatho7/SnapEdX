
'''
This will serve as the default values of a Snap problem.
A method can be used to change these values before Snap is loaded.
'''
problem_config = {
    'snap_width': 1262,
    'snap_height': 600,
    'include_map': False
    #'stage_width': 480,
    #'stage_height': 300
}

